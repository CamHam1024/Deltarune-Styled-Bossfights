function cameraMovement(cameraId, targetX, targetY, cameraSmooth)
{
	//Get current camera Pos
	//camX = camera_get_view_x(cameraId);
	//camY = camera_get_view_y(cameraId);
	//camW = camera_get_view_width(cameraId);
	//camH = camera_get_view_height(cameraId);

	//Set target camera pos
	//moveposX = targetX - camW/2;
	//moveposY = targetY - camH/2;
	
	//Clamp the Target to room bounds
	//targetX = clamp(targetX, 0, room_width - camW);
	//targetY = clamp(targetY, 0, room_height - camH);

	//Smoothly Move the Camera
	//camX = lerp(camX, moveposX, cameraSmooth);
	//camY = lerp(camY, moveposY, cameraSmooth);		
}